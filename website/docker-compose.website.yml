services:
  omi-memory-supabase-web:
    build: ./pocReviewUi
    container_name: omi-memory-supabase-web
    env_file:
      - ./pocReviewUi/.env
    ports:
      - "8097:8097"
    restart: unless-stopped
